<div class="base-container">

  <h1 class="pt-12 p-3 text-2xl">スポット検索</h1>
  <p class="text-sm">
    新しいスポットを見つけましょう！<br>
    ユーザーが登録したスポットを閲覧できます。<br>
  </p>
  
  <div class="base-l text-primary-content">
    <div>条件を指定して検索</div>
    <div class="daisy-collapse">
      <input type="checkbox" id="checkbox" /> 
      <div class="daisy-collapse-title daisy-btn saisy-btn-secondary mt-3 p-3 mx-auto block rounded-full content-center text-sm">
        検索する
      </div>
      <div class="daisy-collapse-content"> 
        <div class="text-center text-secondary-content">
        <%= form_with model: @search_spots_form,
                      url: search_spots_searches_path,
                      method: :get  do |f| %>
          <div class="mt-3 text-lg">
            <%= f.label :area, 'エリア' %>
            <%= f.select :area,
                Spot.areas.keys.map { |key| [t_enum('spot', 'area', key), key] },
                { include_blank: true },
                { class: 'w-64 h-10 px-3 drop-shadow-md rounded-full' } %>
          </div>
          <div class="mt-3">
            <%= f.label :category, 'カテゴリー' %>
            <%= f.select :category,
                Spot.categories.keys.map { |key| [t_enum('spot', 'category', key), key] },
                { include_blank: true },
                { class: 'w-64 h-10 px-3 drop-shadow-md rounded-full' } %>
          </div>
          <div class="mt-3">
            <%= f.label :parking, '駐車場' %>
            <%= f.select :parking,
                  Spot.parkings.keys.map { |key| [t_enum('spot', 'parking', key), key] },
                  { include_blank: true },
                  { class: 'w-64 h-10 px-3 drop-shadow-md rounded-full' } %>
          </div>
          <%= f.submit '検索', class:'daisy-btn daisy-btn-secondary w-20 h-10 mt-3 rounded-full text-base' %>
        <% end %>
      </div>
      </div>
    </div>
  </div>

  <div class="base-m mt-16 mx-auto text-primary-content">
    <p class ="p-1 text-sm">
      もしくは<br>
      検索条件を指定しないで一覧を見る
    </p>
    <%= link_to 'スポット一覧', spots_path,
        class: 'btn-submit' %>
  </div>
</div>

