<div class="base-container">
  <h1 class="h1">お問い合わせ</h1>

  <p class="mt-8">
    下記のお問合せフォームをご入力下さい。<br>
    ご入力いただいたメールアドレスへご回答させていただきます。<br>
    入力内容に誤りが無いようご注意をお願いいたします。
  </p>

  <div class="w-112 mt-3 p-3 mx-auto rounded-2xl border-2">
    <%= form_with model: @contact_form, url: contact_path do |f| %>
      <div>
        <%= f.label :user_id, class: 'text-base' %><br>
        <%= f.text_field :user_id, disabled: true, class: 'daisy-input daisy-input-bordered' %>
        <%= f.hidden_field :user_id %>
      </div>
      <div class="mt-3">
        <%= f.label :email, class: 'text-base' %><br>
        <%= f.text_field :email, class: 'daisy-input daisy-input-bordered bg-white' %>
      </div>
      <div class="mt-3">
        <%= f.label :content, class: 'text-base' %><br>
        <%= f.text_area :content,
            { size: "20x10",
              class: 'daisy-textarea w-4/5 h-40 px-3 drop-shadow-md bg-white' } %>
      </div>
      <%= f.submit '送信', class: 'btn-secondary' %>
    <% end %>
  </div>
</div>