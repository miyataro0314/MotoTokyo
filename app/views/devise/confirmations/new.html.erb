<div class="base-container mb-40">
  
  <h1 class="h1">確認用メールが届かない場合</h1>

  <div class="base-m mt-8 text-primary-content">
    <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div>
        <%= f.label :email, '登録済みのメールアドレス', class: 'font-semibold' %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: 'email', value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email),
            class: 'w-48 h-6 bg-primary border-b' %>
      </div>

      <div class="mt-3">
        <p>
          メールに記載してあるリンクから<br>
          アカウントの有効化をお願いします<br>
        </p>
        <%= f.submit "確認メールを送信する", class: 'btn-secondary' %>
        <p class="mt-3">※ 受信まで最大5~10分ほど時間がかかる場合がございます</p>
      </div>
    <% end %>
  </div>

  <div class="base-m mt-8 text-primary-content">
    <p>
      会員登録がお済みでない方は
    </p>
    <%= link_to '新規会員登録', new_user_registration_path,
        class: 'btn-secondary' %>
  </div>

</div>